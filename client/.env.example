# Base URL the browser uses to reach the API.
# Leave empty to use the same-origin "/api" (recommended when the frontend and
# backend are served behind the same domain / reverse proxy).
# For a split deployment point this at your backend, e.g.:
# VITE_API_URL=https://api.yourcollege.edu/api
VITE_API_URL=/api
